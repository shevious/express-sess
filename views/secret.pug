extends layout

block content
  p Secret Contents for login user
  if user
    h2 Username = #{user}
    h2 cookie = #{sess}
    a(href='/logout')
  else
    h2 Please login
    a(href='/login') LogIn
